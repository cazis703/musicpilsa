# 오늘 할 일

## 진행 중 — 타이틀 재조정 + 배경음 볼륨 조절 구간 균일화

- [x] 사용자 요청: 타이틀 조금 더 진하게, 조금 더 아래로.
  - `SiteTitleBar.tsx`: `text-white/85` → `text-white`(완전 불투명, hover 시 `/80`으로 살짝 옅어짐), 위치 `top-20`(80px) → `top-28`(112px).
- [x] 사용자 요청: 배경음 볼륨 조절 시 0~50%보다 50~100% 구간이 너무 좁게 느껴짐 — 간격 균일화.
  - 원인: 바깥 원(halo) 크기는 볼륨에 순수 선형(vmin)으로 매핑되는데, 실제 렌더링은 px 상한(`HALO_SIZE_CEILING_PX`=220px)에 걸려 있어서 데스크톱 대부분의 화면에서 볼륨 약 76%부터 100%까지는 드래그해도 원 크기가 전혀 안 변하는 "먹통 구간"이었음(순수 계산상 반지름이 이미 상한을 넘어서). 그 결과 상위 구간이 유독 좁게/뻑뻑하게 느껴졌던 것.
  - 수정: `ambient-orb-geometry.ts`에 `haloDisplayRadiusVmin`/`volumeFromHaloRadiusVmin` 추가 — "지금 이 화면에서 실제로 그려질 수 있는 최소~최대 반지름"(px 상한 반영) 사이를 볼륨 0~100%에 다시 선형으로 매핑. 렌더링 크기·히트테스트·드래그 중 볼륨 계산 모두 이 함수로 통일.
  - 검증: Playwright로 볼륨 0/25/50/75/100%의 실제 렌더된 원 반지름을 측정 — 25%씩 늘어날 때마다 약 20~21px씩 균일하게 커지는 것 확인(수정 전엔 75→100% 구간이 거의 0px였을 것). 스크린샷으로 육안 확인, 콘솔 에러 없음.
- [ ] **사용자 브라우저 확인 필요**: 배포 후 실제 사이트에서 타이틀 가독성과 볼륨 조절 구간이 원하는 대로 반영됐는지 확인.

## 완료 (지난 회차, 배포됨)

- 배경음 오브 유영 효과 강화 + 사이트 타이틀 1차 진하게/위치 조정 (`1936e5e`).
- 배경음 오브 볼륨 히트존을 실제 렌더 크기 기준으로 수정 (`7dadfed`) — 100% 근처에서 보이는 원보다 바깥에서부터 조절되던 문제.
- 로딩바 블랙 계열 변경 + 등장 연출 목업 반영 + 기본 폰트 명조로 변경 (`4beeaa5`).
- 타이핑 스파클 위치 이탈 버그 수정(`1a7237b`) — 파티클 캔버스가 로딩 연출용 transform 애니메이션 조상 안에 있어 뷰포트가 아닌 그 조상 기준으로 위치가 잡히던 문제.
- 로딩 연출 속도 조정(`2bf6f3f`, 사용자 피드백: 너무 빠름) — 로딩 최소 노출 시간 0.55→2.2초, 페이드아웃 0.4→0.9초, 요소 순차 등장 딜레이 확대.
- 첫 화면 로딩 연출 최초 도입 + 타이핑 전 배경음악/배경음 재생 안 되던 버그 수정 (`0c1b1b8`).
- 배경 영상 끊김 개선, 배경음 오브 볼륨 조절 UX 개선 — 상세 `tasks/progress.md` 2026-08-26 항목 참고.
- 배경음 오브(사운드 오브) 9종 + Settings 드로어 — 배포 완료.
- 미디어 자산 대대적 압축(565MB→194MB) — 배포 완료.

## 보류 중인 미확정 항목 (사용자 결정: 지금은 건드리지 않음)

- `public/media/(사용안함) 빗소리...wav`(50MB, 코드 미참조) 삭제 여부 → 그대로 둔다.
- `public/media/검색어.txt` 삭제 여부 → 그대로 둔다.
