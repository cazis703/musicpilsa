# 오늘 할 일

## 진행 중 — 배경음 오브 유영 효과 강화 + 사이트 타이틀 진하게/위치 조정

- [x] 사용자 요청: 배경음 오브가 둥둥 떠 있거나 움직이는 느낌을 원함("일단 둥둥뜨게라도"), 성능 영향 여부 확인.
  - 원래도 `orb-bob` CSS 애니메이션(순수 `transform`, GPU 컴포지팅)이 이미 있었지만 진폭이 작고(9px) 모든 오브가 완전히 같은 박자로 움직여서 잘 안 보였음. 성능에는 원래도 지금도 영향 거의 없음(리렌더 없이 브라우저가 합성 레이어에서만 처리).
  - `tailwind.config.ts`의 `orb-bob` 키프레임을 상하 왕복(translateY만)에서 살짝 원을 그리듯 좌우로도 드리프트하도록 확장, 진폭도 소폭 확대.
  - `AmbientOrb.tsx`: 사운드 id를 시드로 오브마다 다른 딜레이(0~3.9s)·재생 속도(7~11s)를 줘서 여러 개를 켜도 제각각 유영하는 것처럼 보이게 함. 위치 드래그(`isDraggingOrb`)뿐 아니라 볼륨 드래그(`haloState === "grabbed"`) 중에도 흔들림을 멈추도록 범위 확장(안 그러면 히트테스트 기준 중심과 실제 흔들리는 위치가 어긋나 조작감이 흔들림).
  - 검증: Playwright로 두 오브의 `transform` matrix를 여러 시점에 샘플링해 서로 다른 값(디싱크)으로 움직이는 것 확인, 스크린샷으로 육안 확인, 콘솔 에러 없음.
- [x] 사용자 요청: 상단 사이트 타이틀이 너무 흐리고 화면 최상단에 붙어있어 잘 안 보임. 진하게 + 위치 조정.
  - 원인: 색상 자체(`text-white/60`)는 안 바뀌었지만, 오늘 기본 폰트를 명조(세리프)로 바꾸면서 획이 가늘어져 같은 투명도에서도 더 흐리게 보였던 것.
  - `SiteTitleBar.tsx`: `text-white/60` → `text-white/85`(hover 시 완전 불투명), 위치 `top-8`(32px) → `top-20`(80px)로 내려서 화면 맨 위 가장자리에 덜 붙어 보이게 조정.
  - 검증: Playwright로 실제 `color`(rgba(255,255,255,0.85))와 `top`(80px) 값 확인.
- [ ] **사용자 브라우저 확인 필요**: 배포 후 실제 사이트에서 오브 유영 느낌과 타이틀 가독성/위치가 원하는 대로 반영됐는지 확인.

## 완료 (지난 회차, 배포됨)

- 배경음 오브 볼륨 히트존을 실제 렌더 크기 기준으로 수정 (`7dadfed`) — 100% 근처에서 보이는 원보다 바깥에서부터 조절되던 문제.
- 로딩바 블랙 계열 변경 + 등장 연출 목업 반영 + 기본 폰트 명조로 변경 (`4beeaa5`).
- 타이핑 스파클 위치 이탈 버그 수정(`1a7237b`) — 파티클 캔버스가 로딩 연출용 transform 애니메이션 조상 안에 있어 뷰포트가 아닌 그 조상 기준으로 위치가 잡히던 문제.
- 로딩 연출 속도 조정(`2bf6f3f`, 사용자 피드백: 너무 빠름) — 로딩 최소 노출 시간 0.55→2.2초, 페이드아웃 0.4→0.9초, 요소 순차 등장 딜레이 확대.
- 첫 화면 로딩 연출 최초 도입 + 타이핑 전 배경음악/배경음 재생 안 되던 버그 수정 (`0c1b1b8`).
- 배경 영상 끊김 개선, 배경음 오브 볼륨 조절 UX 개선 — 상세 `tasks/progress.md` 2026-08-26 항목 참고.
- 배경음 오브(사운드 오브) 9종 + Settings 드로어 — 배포 완료.
- 미디어 자산 대대적 압축(565MB→194MB) — 배포 완료.

## 보류 중인 미확정 항목 (사용자 결정: 지금은 건드리지 않음)

- `public/media/(사용안함) 빗소리...wav`(50MB, 코드 미참조) 삭제 여부 → 그대로 둔다.
- `public/media/검색어.txt` 삭제 여부 → 그대로 둔다.
