# 오늘 할 일

## 진행 중 — 배경음 오브 볼륨 히트존 버그 수정 + 영상/음악 자동재생 조사

- [x] 사용자 요청 1: 사운드 볼륨 100%일 때 실제 바깥 원보다 더 바깥 지점에서부터 조절되는 문제 수정.
  - 원인: 볼륨→반지름 공식(`radiusVminFromVolume`)은 px 상하한을 모르는 순수 vmin 계산인데, 실제 렌더링은 `clamp(46px, Nvmin, 220px)`로 px 상한이 걸려있어 큰 화면·높은 볼륨에서 계산값과 실제 렌더 크기가 어긋났음.
  - 수정: `ambient-orb-geometry.ts`에 `haloVisualRadiusVmin(volume)` 추가(px 상하한까지 반영한 "실제 렌더 크기" 기준 반지름), `AmbientOrb.tsx`의 히트테스트가 이 값을 쓰도록 변경.
  - 검증: Playwright로 실측 — 실제 보이는 원 가장자리(111.8px)에서 커서 반응, 옛 버그 지점(135px)에서는 반응 없음 확인.
- [x] 사용자 요청 2: 진입 시 영상/음악 바로 재생.
  - 조사 결과: 영상은 이미 어떤 상호작용 없이도 자동재생 중(첫 방문자와 동일 조건으로 실측 확인) — 코드 수정 불필요.
  - 음악은 브라우저 자동재생 정책상 첫 제스처 없이는 코드로 재생 보장 불가(모든 사이트 공통 제약). 현재 구현(화면 아무데나 클릭/키 한 번 → 즉시 재생)이 이미 최선이라 별도 수정 없음.
  - "배경효과음이 혼자 재생되던" 건 hwasun님 브라우저의 누적 방문 이력(Media Engagement Index)에 따른 개인 브라우저 특전으로 확인 — 코드가 제약을 우회한 게 아님.
- [ ] **사용자 브라우저 확인 필요**: 배포 후 실제 사이트에서 볼륨 100% 근처 히트존이 원하는 대로 고쳐졌는지 확인.

## 완료 (지난 회차, 배포됨)

- 로딩바 블랙 계열 변경 + 등장 연출 목업 반영 + 기본 폰트 명조로 변경 (`4beeaa5`) — 상세 `tasks/progress.md` 참고.
- 타이핑 스파클 위치 이탈 버그 수정(`1a7237b`) — 파티클 캔버스가 로딩 연출용 transform 애니메이션 조상 안에 있어 뷰포트가 아닌 그 조상 기준으로 위치가 잡히던 문제.
- 로딩 연출 속도 조정(`2bf6f3f`, 사용자 피드백: 너무 빠름) — 로딩 최소 노출 시간 0.55→2.2초, 페이드아웃 0.4→0.9초, 요소 순차 등장 딜레이 확대.
- 첫 화면 로딩 연출 최초 도입 + 타이핑 전 배경음악/배경음 재생 안 되던 버그 수정 (`0c1b1b8`).
- 배경 영상 끊김 개선, 배경음 오브 볼륨 조절 UX 개선 — 상세 `tasks/progress.md` 2026-08-26 항목 참고.
- 배경음 오브(사운드 오브) 9종 + Settings 드로어 — 배포 완료.
- 미디어 자산 대대적 압축(565MB→194MB) — 배포 완료.

## 보류 중인 미확정 항목 (사용자 결정: 지금은 건드리지 않음)

- `public/media/(사용안함) 빗소리...wav`(50MB, 코드 미참조) 삭제 여부 → 그대로 둔다.
- `public/media/검색어.txt` 삭제 여부 → 그대로 둔다.
